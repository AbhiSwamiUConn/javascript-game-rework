**Usage**  
create a python http server in the terminal from within project directory using the below command  
python3 -m http.server 8000  
open the following page  
http://localhost:8000  
close the server using ctrl+c
**Requirements**  
python
# javascript-game-rework
